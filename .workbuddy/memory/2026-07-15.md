# 2026-07-15

## auto-ring：工程骨架已搭好（M1 完成，M2/M3 已落地）
- 完成可打开的 Android 工程：Kotlin + Gradle(KTS) + Jetpack Compose + Room，minSdk26/target34。
- 根级：settings.gradle.kts / build.gradle.kts / gradle.properties / gradle wrapper / .gitignore / README.md / keystore.properties.example。
- app/ 下：AndroidManifest.xml、build.gradle.kts、proguard-rules.pro、res(ic_launcher/themes/strings)。
- 源码 17 个（java/com/example/autoring/）：
  - model: RingerMode, DaysOfWeek
  - data: Profile, Schedule, ProfileScheduleDao, AppDatabase, ProfileRepository
  - audio: AudioProfileManager（AudioManager 闹钟音流 + NotificationManager 铃声模式）
  - scheduler: ProfileScheduler(resolveCurrent/scheduleNext 跨午夜+重叠)、BootReceiver、ProfileAlarmReceiver
  - ui: MainViewModel、MainActivity(+AppRoot 三 Tab)、ProfilesScreen、SchedulesScreen、PermissionGuideScreen
  - AutoRingApplication
- 发布配置已留接口（signingConfigs 注释 + keystore.properties.example + .gitignore）；发布细节见设计文档第12节，暂未做。
- 待办(M4/M5)：手动覆盖"锁定到下次切换"、Realme 真机调优、时间段编辑细化(分钟/逐日勾选)。
- 用户可在 Android Studio 打开 D:\app\auto-ring 跑 debug 包验证。本环境无 SDK/设备，无法代编 APK。
