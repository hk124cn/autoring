# auto-ring 安卓情景音量调度 App — 设计文档

> 目标：按时间段自动切换「闹钟音量 / 来电·通知模式（响铃·静音·震动）/ 震动开关」，
> 解决"戴耳塞睡觉怕睡过头要闹钟最大"与"上班怕打扰别人要静音+震动"的手动切换痛点。
> 支持灵活增删情景模式。

---

## 1. 背景与目标

- **痛点**：睡觉戴耳塞 → 闹钟必须最大；上班开会 → 来电/通知必须静音+震动。手动切换容易忘、容易错。
- **目标**：用户定义若干"情景模式（profile）"，为每个模式设定生效的"时间段 + 星期"，到点自动套用对应策略。
- **范围**：Android 8+（覆盖 ColorOS/Realme），单机型本地运行，无需后台服务器。

---

## 2. 核心可行性：两条独立音流

Android 的音频系统把音量拆成多个 `stream`，与本 App 相关的两条**互不干扰**：

| 音流 | 含义 | 能否独立设置 |
|------|------|------|
| `STREAM_ALARM` | 闹钟音量 | 是，且通常不受勿扰影响 |
| `STREAM_RING` | 来电 + 通知音量 | 是，受铃声模式 / 勿扰策略控制 |

因此"睡眠时闹钟最大、来电静音"能**同时成立**——这是整个功能成立的基础。

---

## 3. 总体架构

```
┌─────────────────────────────────────────────┐
│  UI 层 (Jetpack Compose)                      │
│  模式管理 · 时间段管理 · 当前状态 · 权限引导   │
└───────────────┬─────────────────────────────┘
                │
┌───────────────▼─────────────────────────────┐
│  调度层 ProfileScheduler                      │
│  计算"现在该用哪个模式"，AlarmManager 触发     │
└───────────────┬─────────────────────────────┘
                │
┌───────────────▼─────────────────────────────┐
│  执行层 AudioProfileManager                   │
│  AudioManager + NotificationManager 落地设置  │
└───────────────┬─────────────────────────────┘
                │
┌───────────────▼─────────────────────────────┐
│  数据层 Room：Profile / Schedule              │
│  系统适配层：BootReceiver · 电池豁免 · 保活    │
└─────────────────────────────────────────────┘
```

---

## 4. 数据模型

**Profile（情景模式）**
- `id`
- `name`（如"睡眠""工作""日常"）
- `alarmVolume`（0..max，闹钟音流音量，UI 用 0–100%）
- `ringerMode`（`NORMAL` / `VIBRATE` / `SILENT`）
- `vibration`（bool，勿扰下是否保留震动）
- `priority`（时段重叠时的优先级）

**Schedule（时间段）**
- `id`
- `profileId`（关联模式）
- `startMinute`、`endMinute`（当天分钟数 0..1439；`end < start` 表示跨午夜）
- `daysOfWeek`（星期集合，支持"工作日/周末"差异）
- `enabled`

**存储**：Room（`Profile`、`Schedule` 两张表，1:N 关系）。

---

## 5. 调度引擎

**触发**
- 为每个 Schedule 的起止点用 `AlarmManager` 设置精确闹钟。
- Android 12+：为兼顾上架合规，主用 `SCHEDULE_EXACT_ALARM`（用户需在系统"闹钟和提醒"中授权，Google Play 对"非纯闹钟但确有定时需求"的应用普遍放行）；若未来被认定为纯闹钟/计时类应用，可改 `USE_EXACT_ALARM`（免运行时弹窗）。
- 用 `setExactAndAllowWhileIdle`：保证 Doze 低电耗下也能触发。
- 每次套用某模式后，重算并重排下一次所有切换点（滚动排程）。

**开机重排**
- `BootReceiver`（`RECEIVE_BOOT_COMPLETED`）：开机后（1）重排未来所有闹钟；（2）按当前时间直接补应用应生效的模式（处理关机期间错过的切换）。

**电池优化**
- 引导用户通过 `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 把本 App 加入豁免；ColorOS 还需手动加白名单（见第 9 节）。

**时间解析（跨午夜 / 重叠）**
- `start <= end`：命中条件 `start <= now <= end`。
- `start > end`（跨午夜，如 22:00–07:00）：命中 `now >= start` 或 `now <= end`。
- 多个 Schedule 同时命中：取 `priority` 最高者；相同则取最近编辑的；都无命中 → 套用"默认模式（fallback profile）"。

---

## 6. 执行引擎（落地设置）

`applyProfile(profile)`：
- **闹钟音量**：`audioManager.setStreamVolume(STREAM_ALARM, alarmVolume, 0)`；最大值取 `getStreamMaxVolume(STREAM_ALARM)`。
- **来电/通知模式**：
  - `NORMAL` → `setRingerMode(RINGER_MODE_NORMAL)`
  - `VIBRATE` → `setRingerMode(RINGER_MODE_VIBRATE)`
  - `SILENT` → `setRingerMode(RINGER_MODE_SILENT)`
  - 改到非 `NORMAL` 需 `ACCESS_NOTIFICATION_POLICY` 权限。
- **震动策略**：通过 `NotificationManager.setNotificationPolicy` 配置勿扰策略，决定是否在勿扰下保留震动（`SUPPRESSED_EFFECT_VIBRATION` 等），具体组合在真机实测调优。
- 切换前先读取当前值，无变化则不写，减少系统调用。

---

## 7. 权限清单

| 权限 | 类型 | 用途 |
|------|------|------|
| `MODIFY_AUDIO_SETTINGS` | normal | 修改音量 |
| `ACCESS_NOTIFICATION_POLICY` | 需用户授权 | 改铃声模式 / 勿扰 |
| `SCHEDULE_EXACT_ALARM`（推荐）/ `USE_EXACT_ALARM` | Android 12+ | 精确定时；上架优先前者，需用户在"闹钟和提醒"授权 |
| `RECEIVE_BOOT_COMPLETED` | normal | 开机重排 |
| `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | 引导 | 保活 |
| `POST_NOTIFICATIONS` | Android 13+ 可选 | 常驻状态通知 |
| 厂商自启动/后台白名单 | 手动 | ColorOS 等无法代码强制 |

---

## 8. 交互设计

**主界面**
- 当前生效模式卡片：名称 + 闹钟音量 + 铃声模式 + 震动。
- 模式列表：增 / 删 / 改 / 启停。
- 时间段列表：每个模式下的排程，可增删。
- "立即应用某模式"按钮（手动测试）。

**模式编辑页**：名称、闹钟音量滑块(0–100%)、铃声模式(响铃/震动/静音)、震动开关、优先级。

**时间段编辑页**：开始/结束时间、重复星期（可多选 + 快捷：工作日/周末/每天）、关联模式、启用开关。

**手动覆盖 / 锁定**：提供"锁定到下次切换"开关。启用后用户手动改的音流在本次切换前不被自动覆盖——实现为记录 `overrideUntil`（下次切换时间）与该音流，`applyProfile` 时跳过仍在 override 内的音流。

**权限引导**：首次进入逐项引导跳转到对应系统设置页，并显示 ColorOS 保活图文步骤。

---

## 9. Realme / ColorOS 适配与保活（关键）

ColorOS 后台管理激进，**必须引导用户手动配置，否则定时不触发**：
- 手机管家 → 权限隐私 → 自启动管理：允许本 App 自启动。
- 手机管家 → 电池 → 应用耗电管理 → 本 App：设为"允许后台运行 / 不限制"。
- 设置 → 通知与状态栏 → 通知管理：允许本 App 通知（状态提示用）。
- App 内：权限引导页用截图/步骤说明以上操作；每次 `applyProfile` 失败时提示检查保活。
- 可选：前台服务保活（增加耗电，默认关闭，留作"高可靠模式"开关）。

---

## 10. 边界与异常

- **关机错过切换**：`BootReceiver` 开机即按当前时间补应用。
- **时段重叠**：`priority` 机制（第 5 节）。
- **周末差异**：`daysOfWeek` 字段支持。
- **手动改动冲突**：锁定到下次切换。
- **勿扰已开启时应用模式**：先检测本 App 是否有勿扰权限，否则引导。
- **多用户/工作资料**：仅主用户生效。

---

## 11. 预期项目结构

```
app/
  src/main/
    java/com/example/autoring/
      data/      (Profile.kt, Schedule.kt, AppDatabase.kt, Dao.kt)
      scheduler/ (ProfileScheduler.kt, BootReceiver.kt)
      audio/     (AudioProfileManager.kt)
      ui/        (MainActivity.kt, ProfileScreen.kt, ScheduleScreen.kt, PermissionGuide.kt)
      model/     (枚举与常量)
    res/         (strings, drawables)
    AndroidManifest.xml
  build.gradle, settings.gradle
```
（实际以开工时生成的为准）

---

## 12. 发布与分发 / 市场准备（可安装 + 可上架）

目标：从第一天起就按"正式产品"做，最终产出**带签名的 release 包**，既能直接发给别人安装，也能上架应用市场。

### 12.1 可安装包形态
- **Google Play**：要求 **AAB（Android App Bundle）**，并启用 Play App Signing（谷歌托管分发密钥）。
- **国内渠道 / 直接分发**：**APK** 即可（应用宝、华为、小米、vivo、OPPO·realme 欢太商店，或私下发给朋友）。
- 工程默认支持 `assembleRelease` 同时产出 AAB 与 APK。

### 12.2 签名（关键，私钥自己保管）
- 用 `keytool` 生成发布密钥库（`.jks`），命令随工程文档提供（含 alias、有效期建议 25 年）。
- `app/build.gradle` 配置 `signingConfigs.release`，读取本地 `keystore.properties`（**不入库**，防私钥泄露）。
- Google Play 上架建议移交 Play App Signing：你持"上传密钥"，谷歌持"分发密钥"，换电脑也不怕。
- ⚠️ 密钥一旦丢失无法更新应用，必须离线备份。

### 12.3 版本与构建
- `versionCode`（整数自增）/ `versionName`（如 1.0.0）。
- 开启 R8 / ProGuard 混淆与资源压缩，减小体积、提升逆向门槛。
- 不联网、不收集数据 → 纯本地 App，无后端，分发简单。

### 12.4 应用商店合规要点
- **Google Play**
  - 精确定时：用 `SCHEDULE_EXACT_ALARM`（用户在"闹钟和提醒"授予），Play 对"确有定时需求"的应用放行；不要用 `USE_EXACT_ALARM`（仅限纯闹钟/计时类，审核易拒）。
  - 勿扰：`ACCESS_NOTIFICATION_POLICY` 用于"控制勿扰"核心功能，符合政策。
  - 隐私：本地存储、无联网、无广告 → 隐私政策简单（声明不收集个人信息），需填隐私政策链接与内容分级。
- **国内渠道**（应用宝 / 华为 / 小米 / vivo / OPPO·realme 欢太商店）
  - 均需**实名开发者账号**（个人 or 企业）。
  - 多数主流商店上架**需要《软件著作权登记证书》（软著）**或免责承诺；软著个人可申请，周期约 1–2 个月，建议尽早办。
  - 权限用途须如实声明；上架文案避免"自动防睡过头"等健康/医疗暗示。
- **共性**：提供图标、截图、简介、隐私政策；`RECEIVE_BOOT_COMPLETED` 等权限在商店后台填用途说明。

### 12.5 必须正视的上架风险
- ColorOS/各厂商"杀后台"无法靠代码绕过，必须用户手动加白名单（第 9 节）。意味着**部分用户不按引导设置会觉得"失灵"**，影响商店评分。
- 缓解：App 内强引导 + 首屏"保活自检"；商店简介/FAQ 提前说明；把"已按引导仍失效"做成反馈入口。

### 12.6 直接给朋友装（不上架也行）
- 生成签名 APK → 微信/网盘发送 → 允许"未知来源"安装即可。适合小范围先试用、攒好评再上架。

---

## 13. 风险与缓解

| 风险 | 缓解 |
|------|------|
| 厂商杀后台 | 保活引导 + 忽略电池优化 + 可选前台服务 |
| 勿扰权限被收回 | 启动时检测并重新引导 |
| 精确闹钟权限被关 | 检测并提示去"闹钟与提醒"开启 |
| 通知震动在各 ROM 表现不一 | 以铃声模式 `VIBRATE` 为主、DND 策略为辅，真机调优 |

---

## 14. 开发顺序（里程碑）

- **M1** 数据层 + 简易 UI（增删模式 / 时间段）
- **M2** 调度引擎（AlarmManager + BootReceiver + 跨午夜/重叠）
- **M3** 执行引擎（AudioManager / NotificationManager 落地）
- **M4** 权限引导 + ColorOS 保活 + 手动锁定
- **M5** 真机（Realme）实测调优

---

## 15. 可扩展

- 地理围栏触发（到家/到公司自动切换）
- 与系统日历联动
- 快捷开关 Tile、桌面小组件
- 配置备份/恢复
